<%= link_to gravatar_for(current_user, size: 50), current_user %>
<h1><%= current_user.name %></h1>
<span><%= link_to t("user_info.profile"), current_user %></span>
<span>
  <%= pluralize current_user.microposts.count,
  t("user_info.micropost_singular"), plural: t("user_info.micropost_plural") %>
</span>
