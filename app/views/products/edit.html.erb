<h1><%= t("products.edit_title") %></h1>

<%= render "form", product: @product %>

<br>

<div>
  <%= link_to t("products.show_this"), @product %> |
  <%= link_to t("products.back_to_list"), products_path %>
</div>
