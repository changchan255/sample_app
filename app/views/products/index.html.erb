<p class="notice-message"><%= notice %></p>

<% content_for :title, t("products.title") %>

<h1><%= t("products.title") %></h1>

<table>
  <thead>
    <tr>
      <th><%= t("products.name") %></th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
      <%= render (@products) || t("products.empty") %>
  </tbody>
</table>

<br>

<%= link_to t("products.new"), new_product_path %>
